# Hermes Output Portal (sub-project)

Sub-project living in this repo temporarily. Will be extracted to its own
repo (`hermes-portal`) once the spec is settled.

See [SPEC.md](./SPEC.md) for the full design.

Until extraction, everything related to the portal lives under this
folder so the move is a clean `git mv portal/* ../hermes-portal/`.
